projects:
  - jiraProjectKey: "INTERNAL"
    github:
      owner: "sw-agent-wario"
      repo: "gemoss-clone"
    localRepoPath: "/Users/alfredgenkin/Projects/gemoss-m2"
    upstreamBranch: "production"
    instructions: |
      Magento 2 project.
      yarn start (to start), yarn status (to see containers).
      docker exec <container> to run container commands.
    maxBudgetUsd: 5.00
